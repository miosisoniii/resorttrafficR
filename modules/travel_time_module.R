## travel_time_module.R

travelTimeUI <- function(id) {
  ns <- NS(id)
  
  
  
  
  
}

travelTimeServer <- function(id) {
  moduleServer(id, function(input, output, session) {
    
    
    
    
  })
}